name: ci

on:
  pull_request:
    branches:
      - "main"
    paths-ignore:
      - '**.md'
      - 'examples/**'
      - 'LICENSE'
      - 'CODEOWNERS'
      - 'AUTHORS'
  workflow_dispatch:
    inputs:
      run_spec:
        description: "Run Spec job"
        type: boolean
        required: false
        default: true
      run_acceptance:
        description: "Run Acceptance job"
        type: boolean
        required: false
        default: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  Matrix:
    uses: ./.github/workflows/matrix.yml
    with:
      runs_on: "ubuntu-20.04"
      flags: "--exclude-platforms '[\"Ubuntu-18.04\"]'"
  Spec:
    if: ${{ github.event_name == 'pull_request' || inputs.run_spec == 'true' }}
    name: "Spec"
    needs: Matrix
    uses: ./.github/workflows/module_spec_matrix.yml
    secrets: inherit
    with:
      matrix: ${{ needs.Matrix.outputs.spec_matrix }}
  Acceptance:
    if: ${{ github.event_name == 'pull_request' || inputs.run_acceptance == 'true' }}
    needs: Matrix
    uses: ./.github/workflows/module_acceptance_matrix.yml
    secrets: inherit
    with:
      matrix: ${{ needs.Matrix.outputs.acceptance_matrix }}
